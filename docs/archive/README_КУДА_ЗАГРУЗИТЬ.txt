> Исторический документ. Не применять к beta.3.7. Актуальная инструкция: [развертывание](../DEPLOY.md). Исходные пути и команды относятся к версии на момент написания.

Kitsune v2.3.0-beta.3.5 — готовые файлы для синхронизации GitHub

1. version.json
   Заменить файл version.json в корне репозитория.

2. cloudflare-broker/src/index.js
   Заменить GitHub-копию Worker этим файлом.
   Это НЕ требует повторного Deploy в Cloudflare, если beta.3.5 уже задеплоена:
   файл нужен, чтобы GitHub снова стал источником истины.

Не заменять wrangler.jsonc этим пакетом:
D1-привязка KITSUNE_DB уже создана через Dashboard, а для полной синхронизации
wrangler-конфига нужен точный database_id из Cloudflare.

Рекомендуемый коммит:
v2.3.0-beta.3.5 — Синхронизирована версия и Worker

Описание:
Обновлён version.json до beta.3.5 и GitHub-копия Cloudflare Worker синхронизирована
с развернутой версией регистрации по одобрению владельца.
