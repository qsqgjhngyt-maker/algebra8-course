# Алгебра 8 — интерактивный курс

Версия: **v1.8.0**

Что добавлено в этой итерации:
- живой фон из мягких частиц;
- красивые переливы и glow-эффекты;
- переключатель интенсивности эффектов: авто / мягко / выкл;
- адаптация эффектов под мобильные устройства;
- учёт системной настройки `prefers-reduced-motion`;
- сохранён эталонный формат урока;
- рабочее HTML-превью для быстрого утверждения.

## Запуск
Открыть `index.html` или опубликовать проект на GitHub Pages.

## Публикация на GitHub Pages
1. Создать репозиторий.
2. Загрузить все файлы в корень.
3. Открыть `Settings → Pages`.
4. Выбрать `Deploy from a branch`.
5. Branch: `main`, folder: `/ (root)`.
6. Сохранить настройки.

## Дальше
Следующий этап — полностью наполнить главу I «Рациональные дроби» и затем перейти к «Квадратным корням».


## v0.2.0 — глава I полностью

- 9 полноценных уроков по рациональным дробям;
- в каждом уроке 3 уровня объяснения, ключевая схема, частая ошибка и два пошаговых примера;
- 4 базовых упражнения + задача со звёздочкой на каждую тему;
- индивидуальный процент освоения урока;
- интерактивный график `y = k/x` с ползунком `k`;
- расширенный тренажёр: одинаковые знаменатели, сокращение, умножение, деление, смешанный режим;
- итоговая диагностика на 12 вопросов с рекомендациями;
- мини-контрольная на 6 самостоятельных задач;
- шпаргалка по всей главе;
- сохранение результатов в localStorage.


## v1.0.0 — полный курс

Готовы все 6 глав и 51 тема программы:

1. Рациональные дроби — 9 тем.
2. Квадратные корни — 10 тем.
3. Уравнения и системы — 14 тем.
4. Неравенства — 8 тем.
5. Функции — 5 тем.
6. Степень с целым показателем — 5 тем.

В каждой теме: три уровня объяснения, ключевая схема, «Запомни», «Почему так?», частая ошибка, два пошаговых примера, мини-проверка, четыре самостоятельных задания, задание со звёздочкой и сохранение освоения.

Добавлены интерактивные лаборатории:
- `y = k/x`;
- `y = √x`;
- дискриминант квадратного уравнения;
- числовая прямая для неравенств;
- `y = kx + b`;
- конвертер стандартного вида числа.

Для каждой главы есть диагностика, мини-контрольная и печатная шпаргалка. В конце курса — общая диагностика по всем главам. Тренажёр работает по дробям, корням, квадратным уравнениям, неравенствам и степеням.

Проект остаётся PWA: установка на телефон/ПК, адаптивный интерфейс, офлайн-кэш, локальный прогресс, живой фон и облегчённые эффекты на мобильных устройствах.


## v1.0.0 — полный курс

Полностью реализованы все 6 глав и 51 тема программы:

- глава I «Рациональные дроби» — утверждённая версия;
- глава II «Квадратные корни» — 10 уроков;
- глава III «Уравнения и системы уравнений» — 14 уроков;
- глава IV «Неравенства» — 8 уроков;
- глава V «Функции» — 5 уроков;
- глава VI «Степень с целым показателем» — 5 уроков;
- в каждой новой теме: три уровня объяснения, правила, типичная ошибка, 2 разобранных примера,
  мини-проверка, практика, задание со звёздочкой и итог;
- итоговая диагностика и мини-контрольная для каждой главы;
- универсальный тренажёр по любой главе или всему курсу;
- интерактивные лаборатории: √x, квадратная функция/дискриминант, системы прямых,
  числовые промежутки, линейная функция, 1/x и 1/x²;
- общий прогресс по 51 теме и персональное повторение ошибок.

Содержание написано специально для курса и использует оглавление учебника как карту программы,
а не копирует текст учебника дословно.


## v1.0.1 — интерактивные сноски

- добавлен единый глоссарий сокращений;
- ОДЗ, НОЗ, НОК, D(f), E(f), ℝ, ℚ, ∅ и D (дискриминант в контексте квадратных уравнений)
  получают ненавязчивую пунктирную подсветку и значок ⓘ;
- на компьютере пояснение доступно по наведению/фокусу, на телефоне — по тапу;
- каждая сноска содержит полную расшифровку, короткое объяснение и пример;
- механизм автоматически применяется ко всем текущим и будущим урокам.


## v1.0.1 tooltip fix

Исправлен единственный дефект исходной v1.0.1:
после показа tooltip оставался с inline `display:block`, поэтому визуально не исчезал.
Теперь при уходе курсора класс `.show` снимается и inline display очищается.
Остальная логика и внешний вид v1.0.1 не менялись.


## v1.1.0 — релизная полировка

- утверждённый интерфейс v1.0.1 и точный фикс сносок сохранены;
- контрольные получили уровни «Базовый», «Смешанный», «Уверенный»;
- добавлены визуальные мини-проверки рядом с ключевыми интерактивными графиками;
- экран прогресса показывает общую готовность и слабые темы;
- кнопка установки PWA теперь всегда доступна и показывает инструкции, если браузер не дал native install prompt;
- добавлены `.nojekyll` и инструкция `DEPLOY_GITHUB_PAGES.md`;
- cache Service Worker обновлён до v1.1.0.


## v1.2.0 — педагогическая шлифовка

Утверждённая оболочка v1.1 сохранена. Главы II–VI получили дополнительный педагогический слой во всех 42 темах:

- **«Картинка в голове»** — интуитивный смысл темы;
- **«Короткий маршрут решения»** — алгоритм до начала примеров;
- **«Разберём медленно»** — пошаговая раскрываемая памятка;
- **«Другой взгляд»** — альтернативный способ понять или проверить;
- **«Если застрял»** — конкретное действие, которое помогает продолжить;
- **«Поймай ошибку»** — интерактивный разбор типичной ошибки.

Цель версии — дать ученику возможность самостоятельно восстановить ход решения, а не только повторить готовый образец.


## v1.3.0 — закрепление и подготовка

Добавлен отдельный раздел **«🎓 Закрепление»**, не смешанный с шестью основными главами.

### Повторение 7 класса
8 модулей и 32 оригинальных задания:
- степени и числовые выражения;
- одночлены и многочлены;
- формулы сокращённого умножения;
- разложение на множители;
- линейные уравнения;
- системы линейных уравнений;
- линейная функция;
- текстовые задачи и проценты.

### Диагностика
- входная диагностика — 16 вопросов по фундаменту 7 класса;
- финальная диагностика — 18 вопросов по ключевым навыкам 8 класса;
- сравнение результатов в процентах и рекомендации по слабым темам.

### Итоговое повторение 8 класса
24 смешанных задания, разбитых на 4 станции. Название темы заранее не показывается — ученик должен сам выбрать метод.

### Повышенный уровень
12 задач с кнопкой **«Разобрать идею»**: сначала направление решения, затем проверка и полный разбор.

### Контрольные
3 варианта по 18 заданий:
- базовый — 45 минут;
- смешанный — 50 минут;
- итоговый — 60 минут.

Таймер включается по желанию. После сдачи показываются результат, решения ошибок и ссылки на темы для повторения.

Все результаты дополнительного раздела сохраняются локально на устройстве.


## v1.4.0 — два дизайна

Добавлен переключаемый режим оформления.

### 🎮 Игровой — основной
Новый основной интерфейс вдохновлён современными игровыми образовательными приложениями:
- чистый светлый фон без glassmorphism;
- яркий зелёный учебный акцент;
- крупные округлые карточки;
- «тактильные» кнопки с визуальной глубиной;
- более выраженные полосы прогресса;
- спокойная игровая микроанимация;
- адаптация всех экранов: уроки, карта курса, тренажёры, контрольные, «Закрепление» и прогресс.

Это самостоятельный визуальный стиль курса: фирменные персонажи, логотипы и графические элементы сторонних сервисов не копируются.

### ✨ Классический
В один клик возвращается исходный утверждённый интерфейс курса с синим градиентом, glassmorphism, живым фоном и прежними эффектами.

Переключатель находится в нижней части боковой панели:
**«🎮 Дизайн: игровой» / «✨ Дизайн: классический»**.

Выбор сохраняется в `localStorage` (`a8_design_mode`) и восстанавливается при следующем запуске.


## v1.4.1 — Game FX

Для переключателя **«🎮 Дизайн: игровой»** добавлена отдельная анимация:

- мягкое циклическое зелёное свечение;
- бегущий световой блик;
- плавающие мини-искры;
- лёгкое движение значка 🎮;
- усиленный hover-эффект;
- короткий burst частиц при переключении;
- праздничный импульс при возвращении в игровой режим;
- поддержка `prefers-reduced-motion`.

В классическом дизайне эти эффекты полностью отключены.


## v1.4.2 — Game Learning FX

Игровой дизайн получил награды прямо внутри обучения:

- **правильный ответ** — короткий pop карточки, зелёная отметка и локальные частицы;
- добавлены **настоящие XP**, которые сохраняются в `localStorage`;
- обычное задание впервые даёт **+10 XP**, мини-проверка **+5 XP**, сложная задача — до **+20 XP**;
- один и тот же элемент нельзя бесконечно использовать для набора XP;
- общий счёт XP виден в верхней панели только в игровом дизайне;
- полосы прогресса теперь плавно заполняются и получают короткий световой блик;
- первое завершение урока даёт **+50 XP** и короткую экранную награду;
- полное завершение главы дополнительно даёт **+150 XP** и более крупное празднование с конфетти;
- после завершения всех шести основных глав появляется отдельная награда;
- системная настройка `prefers-reduced-motion` отключает анимации;
- **классический дизайн остаётся без игровых эффектов**.

Полный сброс учебного прогресса также сбрасывает игровые XP.


## v1.5.0 — живой помощник «Kitsune»

В игровой дизайн добавлен собственный интерактивный SVG-персонаж курса — **Kitsune**.

### Что умеет Kitsune

- знает, какой урок и экран сейчас открыт;
- использует `remember`, `formula`, `summary`, `mistake` и простое объяснение именно текущей темы;
- даёт короткий контекстный совет по нажатию **«💡 Совет»**;
- кнопка **«🌱 Объясни проще»** переводит урок на самый простой уровень объяснения;
- реагирует на правильные и неправильные ответы;
- замечает серию правильных решений;
- после двух ошибок подряд предлагает замедлиться и проверяет типичную ошибку текущей темы;
- меняет поведение на контрольных, диагностике, экране ошибок и прогресса;
- реагирует на завершение урока;
- после бездействия переходит в спокойное «спящее» состояние;
- один раз в день приветствует ученика;
- умеет прочитать текущий совет вслух через системный `SpeechSynthesis`, если браузер его поддерживает.

### 9 анимационных состояний

`idle`, `wave`, `think`, `explain`, `happy`, `oops`, `focus`, `cheer/celebrate`, `sleep`.

Глаза Kitsune мягко следят за указателем мыши на компьютере.

### Режимы

В боковой панели и в настройках самого помощника доступны:

- **✨ Активный** — реакции, советы и поддержка по ходу обучения;
- **💡 Только советы** — без постоянных поздравлений; Kitsune вмешивается по запросу и при повторной ошибке;
- **🙈 Выкл.** — персонаж полностью скрыт.

Выбор сохраняется в `localStorage` (`a8_assistant_mode`).

**Классический дизайн не изменён:** Kitsune визуально показывается только в игровом режиме.


## v1.5.1 — эмоциональный голос Kitsune

Озвучка помощника переработана в отдельный голосовой профиль без копирования голоса какого-либо существующего персонажа.

### Голосовые профили
- **⚡ Живой** — основной: молодой, звонкий, энергичный и немного мультяшный;
- **🌿 Мягкий** — спокойнее для объяснений и поддержки;
- **📘 Чёткий** — медленнее и нейтральнее для учебного текста.

### Эмоции
Скорость и высота системного русского голоса меняются по состоянию Kitsune:
`wave`, `think`, `explain`, `happy`, `oops`, `focus`, `cheer`, `celebrate`, `sleep`.

### На телефоне
Курс получает список русских голосов, установленных в Android/iOS и браузере. При автовыборе предпочитается локальный русский голос, а при наличии подходящих вариантов — более яркий женский голос. Пользователь может выбрать любой доступный голос вручную.

### Настройки
В окне Kitsune → ⚙:
- включить/выключить голос;
- выбрать профиль;
- выбрать конкретный русский голос устройства;
- включить **автоозвучку**;
- нажать **«▶ Послушать Kitsune»**.

Автоозвучка по умолчанию выключена, чтобы курс не начинал неожиданно говорить. После её включения Kitsune сам озвучивает важные советы и короткие реакции. На мобильных устройствах первый звук запускается после пользовательского нажатия — это ограничение браузеров.

Если Web Speech API отсутствует, курс продолжает работать без озвучки.


## v1.6.0 — AI Tutor Lite

В Kitsune встроен локальный интеллектуальный математический тьютор, который работает **полностью в браузере, офлайн и без API**.

### Как он работает

Tutor Lite получает только локальный контекст курса:

- `lessonId` текущей темы;
- условие выбранного упражнения;
- введённый учеником ответ;
- допустимые правильные ответы;
- формулу, правило, блок `remember`, `why`, типичную ошибку и подсказку урока;
- накопленную локальную память ошибок ученика.

### Лестница помощи

Для каждого упражнения есть 4 уровня:

1. **💡 Намёк** — направление без раскрытия ответа;
2. **👣 Первый шаг** — с чего начать решение;
3. **🔎 Явная подсказка** — правило/формула и место, которое нужно проверить;
4. **📘 Полный разбор** — алгоритм и ожидаемый итог.

Кнопка **↺ Сначала** закрывает лестницу, чтобы попробовать задачу заново самостоятельно.

### «Почему?»

Tutor Lite использует объяснение смысла правила текущей темы, а не только сообщает алгоритм.

### «Проверь мой ход»

Можно написать промежуточные действия или итоговый ответ. Tutor Lite анализирует последний шаг и пытается определить класс ошибки.

### Распознаваемые типичные ошибки

Локальный движок различает, в частности:

- смену знака при делении неравенства на отрицательное число;
- потерю знака числа;
- ОДЗ и запрещённые значения;
- действия с рациональными дробями;
- квадратные корни;
- дискриминант и квадратные уравнения;
- теорему Виета;
- системы уравнений;
- интервалы и границы;
- свойства степеней;
- стандартный вид числа;
- область определения и свойства функций;
- арифметические ошибки;
- общий неверный ход решения.

### Память ошибок

Tutor Lite хранит статистику только локально в `localStorage`:

- `a8_tutor_memory_v16`;
- `a8_tutor_help_v16`.

Если одна ошибка повторяется, Kitsune сообщает об этом и делает следующую помощь проще. При нескольких затруднениях по теме первый уровень подсказки автоматически переходит к более простому объяснению урока.

Полный сброс курса очищает и эту память.

### Интерфейс

В каждом упражнении игрового дизайна появляется кнопка:

**🧠 Kitsune Tutor**

Также кнопка **🧠 Tutor Lite** доступна прямо в окне Kitsune.

Классический дизайн визуально не изменён.

### Архитектура

`GitHub Pages → PWA → курс → Tutor Lite → Kitsune → голос/анимации`

Никаких внешних запросов, API-ключей, серверов или платных сервисов.


## v1.6.1 — более живая озвучка Kitsune

Системный Web Speech остаётся полностью локальным и бесплатным, но теперь Kitsune не читает всю реплику одним монотонным `SpeechSynthesisUtterance`.

### Что изменено

- длинная реплика разбивается на короткие смысловые фразы;
- каждая фраза получает свою скорость и высоту тона;
- между фразами появились небольшие «дыхательные» паузы;
- вопрос, восклицание, объяснение, поддержка и концентрация озвучиваются по-разному;
- в конце мысли голос немного замедляется и опускается;
- чрезмерно высокий постоянный `pitch` убран — это уменьшает «роботизированность»;
- добавлена микро-вариативность интонации между соседними фразами;
- математические символы предварительно переводятся в нормальную русскую речь:
  `√`, `≥`, `≤`, `≠`, `x`, `y`, `ОДЗ`, `НОЗ`, степени и знаки сравнения;
- улучшено ранжирование доступных голосов: выше ставятся `Natural`, `Neural`, `Enhanced`, Google, Microsoft и Samsung;
- кнопка **«🎧 Сравнить лучшие голоса»** по очереди проигрывает до трёх лучших вариантов устройства;
- появился регулятор **«✨ Живость» 0–100%**;
- профиль **«⚡ Живой+»** теперь использует умеренный базовый pitch и динамическую эмоциональную режиссуру;
- сохранены профили **«🌿 Мягкий»** и **«📘 Чёткий»**;
- на кнопке озвучки появляется мини-индикатор речи.

### Важно

Качество самого тембра всё равно зависит от TTS-голосов, установленных в Android, iOS, Windows или браузере. v1.6.1 заметно улучшает подачу и интонацию, но не превращает старый системный синтезатор в нейросетевой облачный голос.

Все настройки работают без API и сохраняются локально.


## v1.7.0 — Neural Voice Kitsune

Kitsune получил второй голосовой движок: **локальный нейросетевой Piper TTS**.

### Режимы

- **✨ Нейро** — локальная нейросетевая генерация;
- **📱 Системный** — прежний Web Speech API;
- внутренний режим `auto` использует Neural Voice, если модель подготовлена, иначе автоматически оставляет системный fallback.

### Первый запуск

Нейромодель не включена в ZIP проекта. Это сделано специально: файл около **63 МБ**, а курс обычно обновляется через веб-интерфейс GitHub.

В настройках Kitsune нажмите:

**⬇ Скачать голос Kitsune (~63 МБ)**

После загрузки библиотека сохраняет модель в Origin Private File System браузера. Затем выполняется короткая пробная генерация, которая подготавливает ONNX/phonemizer runtime.

### Основа голоса

- Piper voice: `ru_RU-dmitri-medium`;
- quality: medium;
- sample rate: 22,050 Hz;
- модель Piper Voices;
- датасет, указанный в model card: CC0.

Это не копия голоса какого-либо мультяшного персонажа. Поверх нейроголоса курс применяет собственную лёгкую обработку.

### «Мультяшность»

Регулятор **🎬 Мультяшность 0–100%** управляет:

- небольшим изменением pitch/detune;
- playback rate;
- присутствием голоса в среднем диапазоне;
- яркостью верхних частот;
- динамической компрессией;
- эмоциональными пресетами Kitsune.

Настройки специально ограничены умеренным диапазоном, чтобы речь оставалась человеческой и разборчивой.

### Эмоции

Для состояний `wave`, `think`, `explain`, `happy`, `oops`, `focus`, `cheer`, `celebrate`, `sleep` используются разные скорость, detune и яркость.

### Offline

Архитектура:

`GitHub Pages → PWA → Piper runtime → модель в OPFS → Web Audio → Kitsune`

После первого успешного скачивания и пробного запуска:

- модель хранится локально в OPFS;
- jsDelivr runtime/WASM кэшируется Service Worker;
- основной PWA app shell кэшируется отдельно;
- большие model responses Hugging Face не дублируются в Cache Storage.

При любой ошибке Neural Voice автоматически откатывается на системную озвучку.

### Зависимость

Neural Voice лениво загружает браузерную библиотеку:

`@mintplex-labs/piper-tts-web@1.0.5`

Она используется только при скачивании/запуске Neural Voice. Обычный курс, Tutor Lite и системная озвучка не зависят от неё.


## v1.7.1 — FIX Neural Voice

Исправлен сценарий, при котором загрузка нейроголоса доходила до 100%, но интерфейс снова показывал «нейроголос ещё не скачан».

### Что исправлено

- успешное скачивание модели и успешный warm-up теперь считаются двумя разными состояниями;
- после `tts.download()` готовность подтверждается через `tts.stored()` в OPFS;
- если первый `tts.predict()` не запустился, модель **не помечается удалённой** и не предлагается качать 63 МБ заново;
- настоящий текст ошибки больше не затирается финальным `updateUi()`;
- при старте v1.7.1 автоматически ищет модель, которую могла уже скачать v1.7.0;
- добавлена кнопка **🔎 Проверить модель**;
- если модель найдена, активируются **Тест нейроголоса** и **Удалить модель**;
- при ошибке генерации остаётся системный fallback.

Если пользователь уже скачал модель в v1.7.0 на том же GitHub Pages origin, после обновления до v1.7.1 она обычно будет обнаружена без повторной загрузки.


## v1.7.2 — UI FIX Kitsune и подсказок

Исправлены два UX-багa.

### 1. Высокое меню Kitsune

После добавления настроек системного голоса и Neural Voice панель могла стать выше окна браузера.

Теперь:

- высота панели ограничивается реальной высотой viewport (`100dvh`);
- внутри панели появился вертикальный скролл;
- поддержан touch-scroll / momentum scroll на смартфонах;
- прокрутка не уходит в страницу (`overscroll-behavior: contain`);
- верхняя строка **Kitsune / 🔊 / ⚙ / ×** остаётся закреплённой;
- для узких смартфонов используются отдельные ограничения высоты;
- добавлен тонкий scrollbar на ПК.

### 2. Штатная «💡 Подсказка»

Раньше глобальный обработчик Kitsune реагировал на слово «Подсказка» и открывал меню помощника поверх штатной подсказки урока.

Теперь кнопки курса:

- **💡 Подсказка**;
- **Хочу понять**;
- **Разобрать идею**;

не перехватываются Kitsune и выполняют только своё штатное действие.

Кнопки **Совет / Tutor Lite** внутри самого Kitsune продолжают работать через собственные обработчики.


## v1.7.3 — Smart Tutor FIX

Исправлена логика помощи Kitsune после проверки задания.

### Больше никаких настроек после «Проверить»

Нажатие обычной кнопки **«Проверить»** теперь никогда не открывает окно или настройки Kitsune.

При неправильном ответе:
- штатная проверка остаётся на месте;
- под конкретным упражнением автоматически появляется небольшой inline-разбор;
- Kitsune может показать только короткий ненавязчивый chip возле персонажа;
- большой голосовой/настроечный интерфейс не открывается.

### Помощь теперь внутри задания

Кнопка **🤖 Разобрать с Kitsune** открывает помощь прямо под выбранным примером.

Inline-разбор содержит:
- введённый ответ и конкретную диагностику;
- первый шаг именно для текущего примера;
- кнопку **👣 Следующий шаг**;
- **❓ Почему так?**;
- **✍️ Проверить мой ход**;
- **👁 Показать ответ** только по явному нажатию.

Ответ не показывается автоматически.

### Более конкретный математический движок

Smart Tutor пытается строить реальные промежуточные шаги, а не фразы «давай разложим решение».

Добавлены специальные локальные разборы для:
- линейных неравенств с вычислением границы и переворотом знака;
- квадратных уравнений с вычислением a, b, c, D и корней;
- квадратных корней;
- дробных выражений и ОДЗ;
- степеней и стандартного вида числа;
- функций и области определения.

Для остальных заданий он использует конкретную штатную подсказку упражнения, правило урока и шаги разобранного примера этой темы.

### Меню Kitsune стало короче

Старая большая панель Tutor Lite внутри настроек Kitsune больше не создаётся. Вместо неё добавлена одна компактная кнопка:

**🧠 Разобрать текущее**

Она закрывает окно Kitsune и переносит ученика к inline-разбору текущего упражнения.


## v1.8.0 — Kitsune Brain

Главный персонаж курса полностью заменён: вместо Альфи теперь используется оригинальный лисёнок **Kitsune**.

### Новый характер интерфейса

Kitsune появляется возле конкретного упражнения как персонаж из комикса и разговаривает через речевое облачко. При ошибке обычная кнопка **«Проверить»** не открывает настройки помощника.

Комиксный блок показывает:
- где именно разошёлся ход решения;
- какой промежуточный шаг был верным;
- какую конкретную операцию нужно проверить дальше;
- наводящий вопрос без автоматического раскрытия ответа;
- реакцию на повторяющуюся ошибку.

### Гибридный мозг

Архитектура:

`Math Engine → verified facts → Kitsune Brain → comic bubble`

**Math Engine / Smart Tutor** остаётся источником математической истины.

Локальная LLM **не имеет права самостоятельно менять вычисления**. Ей передаются уже проверенные факты: тип ошибки, следующий корректный шаг, правило темы и повторяемость ошибки. Модель используется только для естественной русской формулировки и сократического диалога.

### Локальная модель

Kitsune Brain использует WebLLM и компактную модель:

`Qwen2.5-0.5B-Instruct-q4f16_1-MLC`

- inference выполняется в браузере через WebGPU;
- внешний inference API отсутствует;
- API-ключи отсутствуют;
- модель скачивается только после явного нажатия пользователя;
- после подготовки WebLLM использует локальный browser cache;
- при отсутствии WebGPU или ошибке модели автоматически работает Smart Tutor;
- WebLLM указывает для этой quantized-конфигурации около 945 МБ требуемой VRAM.

### Режимы

В настройках Kitsune:

- **🧠 Brain** — Math Engine + локальная LLM;
- **⚙ Smart Tutor** — только детерминированный математический движок;
- **💡 Только подсказки** — без автоматического inline-разбора после ошибки.

По умолчанию используется Smart Tutor. Никакая большая модель не скачивается автоматически.

### «Проверить мой шаг»

В комиксном разборе можно написать промежуточную строку, например:

`−4x≥−8`

или

`D=16`

Kitsune сравнивает её с проверенной цепочкой решения. Для поддерживаемых типов задач движок различает корректный промежуточный шаг, ошибочный знак неравенства, неверный дискриминант и другие типовые расхождения.

### Память

Kitsune ведёт отдельную локальную память повторяющихся ошибок:

`a8_kitsune_brain_memory`

Она используется для адаптации реплик и хранится только в браузере.

### Голос

Системная и Neural Voice озвучка сохранены, но весь видимый интерфейс теперь относится к **Kitsune**.

### Совместимость

Kitsune Brain проверяет:
- наличие WebGPU;
- возможность получить GPU adapter;
- приблизительный объём RAM через `navigator.deviceMemory`, если браузер его сообщает;
- доступное browser storage.

Если устройство слабое, курс не блокируется: используется Smart Tutor.
