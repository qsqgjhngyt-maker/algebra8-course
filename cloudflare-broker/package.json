{
  "name": "kitsune-hybrid-broker",
  "version": "2.3.0-beta.3.7",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --test tests/broker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
