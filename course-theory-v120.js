/* Kitsune · Алгебра 8 · Полная теория для всех 51 уроков · v1.2.0 */
(() => {
  "use strict";

  const VERSION="1.3.1";
  const chapterContext={
    1:"Первая глава формирует привычку работать с алгебраическими дробями осмысленно: следить за допустимыми значениями, видеть структуру числителя и знаменателя и выбирать преобразование, которое действительно упрощает выражение.",
    2:"Глава о квадратных корнях расширяет представление о числах и учит переходить между точным значением, приближением, формулой и графиком. Здесь особенно важно понимать смысл каждого преобразования, а не только применять правило.",
    3:"Уравнения и системы — это язык математических моделей. Почти каждое правило этой главы нужно не ради самой формулы, а чтобы превращать условие задачи в последовательность равносильных шагов и получать проверяемый ответ.",
    4:"Неравенства описывают не одно число, а целое множество допустимых значений. Поэтому в этой главе важно одинаково уверенно работать со знаком неравенства, промежутками и числовой прямой.",
    5:"Функции связывают формулу и график. Цель главы — научиться видеть за записью поведение зависимости: какие значения допустимы, где функция растёт или убывает и что можно прочитать по графику.",
    6:"Степень с целым показателем помогает компактно работать с очень большими и очень малыми величинами. Здесь правила степеней становятся инструментом вычислений, оценки порядка и чтения научной записи."
  };

  const clean=s=>{
    if(window.KitsuneSchoolNotation?.html)return window.KitsuneSchoolNotation.html(s);
    return String(s??"").replace(/<[^>]+>/g," ").replace(/\u00F7/g,":").replace(/\u00D7/g,"·").replace(/\s+/g," ").trim();
  };
  const list=x=>Array.isArray(x)?x:[];
  const examples=d=>{
    if(Array.isArray(d.examples)&&d.examples.length)return d.examples;
    if(d.example)return [d.example];
    return [];
  };
  const goals=d=>{
    if(Array.isArray(d.goals)&&d.goals.length)return d.goals;
    const base=[];
    if(d.remember)base.push("понять и применять ключевое правило темы");
    if(d.why)base.push("объяснять, почему выбранное преобразование допустимо");
    base.push("проверять решение и замечать типичную ошибку");
    return base.slice(0,3);
  };
  const summary=d=>{
    if(Array.isArray(d.summary)&&d.summary.length)return d.summary;
    return [d.remember,d.why].filter(Boolean);
  };

  function chapterOf(id){
    try{return chapters.find(ch=>ch.topics.some(t=>t.id===id))}catch{return null}
  }

  function html(id,d){
    if(!d)return "";
    const ch=chapterOf(id),ctx=chapterContext[ch?.id]||"Эта тема входит в общую систему курса и связывает правило, способ рассуждения и практическое применение.";
    const school=d.levels?.school||d.levels?.simple||d.lead||"";
    const deep=d.levels?.deep||school;
    const ex=examples(d);
    const g=goals(d);
    const sum=summary(d);
    const formula=d.formula||d.remember||"Сначала сформулируй правило словами, затем переведи его в математическую запись.";

    return `<section class="v12-full-theory v12-deep-dive reveal" data-full-theory="${id}" aria-hidden="true">
      <div class="v12-full-theory-head">
        <div><span class="eyebrow">🧠 Углублённый разбор</span><h3>Разбираемся глубже: почему это работает</h3><p class="muted">Логика → связи → обоснование → стратегия → примеры → типичные ловушки → вывод.</p></div>
        <span class="status-chip">урок ${id}</span>
      </div>

      <div class="v12-theory-grid">
        <article class="v12-theory-card">
          <span class="eyebrow">1 · Смысл темы</span>
          <h4>Что именно мы изучаем</h4>
          <p>${clean(d.lead)}</p>
          <p>${clean(school)}</p>
          <p>${ctx}</p>
          <p>Важно не пытаться запомнить тему как набор отдельных команд. Сначала ответь себе на два вопроса: <b>какой объект здесь меняется</b> и <b>какое свойство позволяет выполнить следующий шаг</b>. Если эти два вопроса понятны, формула перестаёт быть механической.</p>
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">2 · Главная идея</span>
          <h4>Почему правило работает</h4>
          <p>${clean(deep)}</p>
          <p>${clean(d.why||"Каждое преобразование опирается на уже известное свойство чисел, выражений, уравнений или функций. Поэтому полезно проговаривать не только действие, но и основание для него.")}</p>
          <div class="v12-theory-formula"><b>Ключевая схема</b><div>${clean(formula)}</div></div>
          <p>Когда видишь подобную задачу, не спеши сразу считать. Сначала сопоставь условие с этой схемой, проверь ограничения и только потом выполняй вычисления. Такой порядок особенно помогает в длинных примерах, где одна ранняя ошибка тянет за собой все остальные.</p>
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">3 · Рабочий алгоритм</span>
          <h4>Как рассуждать по шагам</h4>
          <ol class="v12-theory-steps">
            <li><b>Определи тип задачи.</b> Посмотри на запись и назови тему своими словами: «я работаю с ${clean(d.title).toLowerCase()}».</li>
            <li><b>Зафиксируй ограничения и исходные данные.</b> Не начинай преобразования, пока не понятно, какие значения допустимы и что именно нужно найти.</li>
            <li><b>Выбери одно действие.</b> На каждом шаге меняй только то, что необходимо, и сохраняй смысл исходной записи.</li>
            <li><b>Проверь результат.</b> Подстановка, обратное действие, оценка порядка величины или чтение графика часто позволяют поймать ошибку сразу.</li>
          </ol>
          <div class="callout good"><b>Цели урока</b><br>${g.map(x=>`• ${clean(x)}`).join("<br>")}</div>
          <p><b>Запомни:</b> ${clean(d.remember||"лучший алгоритм — тот, в котором ты понимаешь назначение каждого шага.")}</p>
        </article>

        <article class="v12-theory-card v12-theory-wide">
          <span class="eyebrow">4 · Ход решения</span>
          <h4>Как читать пример, а не просто переписывать его</h4>
          ${ex.length?ex.slice(0,2).map((e,i)=>`<div class="v12-example">
            <b>Пример ${i+1}. ${clean(e.task||"Разберём применение правила.")}</b>
            <ol>${list(e.steps).map(step=>`<li>${clean(step)}</li>`).join("")}</ol>
            <p class="muted">После каждого шага задай себе вопрос: «что изменилось и какое правило это разрешает?». Такой комментарий превращает готовое решение в навык, который можно перенести на новую задачу.</p>
          </div>`).join(""):`<p>Для этой темы основной разбор встроен в интерактивную часть урока. Пройди его медленно: сначала предскажи следующий шаг сам, а только потом сравни с подсказкой.</p>`}
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">5 · Контроль ошибки</span>
          <h4>Где чаще всего сбиваются</h4>
          <div class="callout danger"><b>⚠️ Типичная ошибка</b><br>${clean(d.mistake||"Выполнить знакомое действие автоматически, не проверив, подходит ли оно к текущей записи.")}</div>
          <p>Хорошая привычка — после решения посмотреть не только на ответ, но и на самый «опасный» переход. Обычно это место со знаком, знаменателем, корнем, степенью, раскрытием скобок или переходом от формулы к графику.</p>
          <p>Если ответ выглядит странно, не начинай всё заново. Вернись к последнему шагу, который ты можешь объяснить словами, и продолжай проверку от него.</p>
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">6 · Как объяснить себе самому</span>
          <h4>Перескажи тему своими словами</h4>
          <p>Попробуй временно забыть формулу и объяснить идею обычными словами. Что дано? Что разрешено делать? Что меняется после каждого шага? Как понять, что результат правдоподобен? Такой пересказ помогает заметить пробелы, которые не видны при простом перечитывании.</p>
          <p>Хорошая самопроверка звучит так: <b>«Я могу решить пример и могу объяснить, почему именно этот шаг допустим».</b> Если получается только первое, тему стоит прочитать ещё раз именно в части «Почему так?».</p>
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">7 · Связь с предыдущими темами</span>
          <h4>На чём держится этот урок</h4>
          <p>Новая тема почти никогда не возникает изолированно. Здесь используются уже знакомые действия с числами, выражениями, уравнениями или графиками. Чем увереннее базовые преобразования, тем меньше приходится держать в памяти одновременно.</p>
          <p>Перед сложной задачей полезно назвать знакомые элементы: знак, степень, знаменатель, корень, коэффициент, область допустимых значений, координаты или промежуток. После этого задача обычно распадается на несколько маленьких знакомых шагов.</p>
          <div class="callout"><b>Связь с курсом</b><br>${clean(ctx)}</div>
        </article>

        <article class="v12-theory-card">
          <span class="eyebrow">8 · Мини-конспект</span>
          <h4>Что должно остаться после урока</h4>
          <ul>${sum.map(x=>`<li>${clean(x)}</li>`).join("")}</ul>
          <p>После чтения закрой этот блок и попробуй своими словами объяснить тему за одну минуту. Если можешь назвать правило, привести один пример и предупредить об одной ошибке — теория уже начала превращаться в навык.</p>
        </article>
      </div>
    </section>`;
  }

  window.KitsuneFullTheory8={version:VERSION,html};
})();