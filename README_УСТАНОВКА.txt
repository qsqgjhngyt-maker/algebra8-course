Kitsune v2.3.0-beta.3.6.2 — Админ-панель и стабильный диалог

ЧТО ИСПРАВЛЕНО

1. Админ-панель больше не зависает молча на «Загрузка…».
   - запросы admin имеют таймаут 15 секунд;
   - серверная ошибка показывается прямо в панели;
   - есть кнопки «Повторить» и «Проверить Owner»;
   - D1-схема создаётся идемпотентными отдельными запросами;
   - права Owner проверяются до работы с D1.

2. Диалог Kitsune больше не должен выбрасывать на Главную.
   Предыдущая защита ориентировалась на активную вкладку меню.
   Внутри урока активной вкладки нет, поэтому защита не включалась.
   beta.3.6.2:
   - блокирует window.go("home");
   - блокирует KitsuneAppKernel.route("home");
   - блокирует прямой renderHome();
   - запоминает текущий lessonId;
   - при обходном DOM-переходе восстанавливает исходный урок/раздел;
   - firewall загружается ПОСЛЕДНИМ, после App Kernel.

УСТАНОВКА

Cloudflare:
- полностью заменить код Worker содержимым cloudflare-worker-index-v2362.js;
- Deploy;
- KITSUNE_DB и Secrets НЕ менять.

GitHub, корень — заменить:
- index.html
- sw.js
- version.json
- access-admin-v235.js
- chat-dialog-firewall-v231.js

hybrid-infrastructure-v230.js можно не заменять, если уже стоит beta.3.6.1.
Он включён в пакет только для полноты.

GitHub:
- заменить cloudflare-broker/src/index.js на файл из пакета.

НЕ очищать IndexedDB, OPFS, прогресс и локальные модели.

ПРОВЕРКА

Админка:
- открыть 🛡️ Администрирование.
- Ожидается список и счётчики.
- Если Worker/D1 вернут ошибку, она теперь будет видна вместо вечной «Загрузка…».

Диалог:
- открыть урок;
- открыть диалог Kitsune;
- отправить сообщение;
- дождаться полного ответа;
- диалог и текущий урок должны остаться на месте.

НАЗВАНИЕ КОММИТА
v2.3.0-beta.3.6.2 — Исправлена админ-панель и стабильность диалога

ОПИСАНИЕ
Устранено зависание админ-панели на экране загрузки: добавлены таймаут, повторная загрузка и отображение серверной ошибки, а инициализация D1 сделана более надёжной. Усилена защита диалога Kitsune: во время ответа блокируется непреднамеренный переход на главную как в разделах приложения, так и внутри урока.
